# مسودة مشروع Riot — الحالة الكاملة

**المستودع**: https://github.com/ali12hhh-oss/Riot
**المحرك**: Godot 4.3
**النوع**: لعبة أكشن/قيادة بعالم مفتوح (أسلوب GTA) — قصة Zade و Nyra

> هذا الملف هو المرجع الحي لحالة المشروع. أي تعديل جوهري (إصلاح خطأ، ميزة جديدة، تغيير بنية) يُسجَّل بقسم **"سجل التعديلات"** بالأسفل بالتاريخ.

---

## 1. الشخصيات

| الشخصية | الجنس | الميزة الخاصة | النموذج |
|---|---|---|---|
| **Zade** | ذكر | +20 صحة إضافية | Universal Base Characters (Quaternius) |
| **Nyra** | أنثى | سرعة أعلى (x1.15) + تخفي أعلى (+25%) | Universal Base Characters (Quaternius) |

النموذجين مرتبطين بمكتبة حركات حقيقية (`assets/characters_v2/animations.glb`) فيها 42 حركة (وقوف، جري، قفز، تسلل، إطلاق مسدس، ضربة سيف، سباحة، لكم، موت...) — مفعّل منها حالياً: `idle`, `run`, `jump` (البقية جاهزة بالمكتبة بس مو مربوطة بمنطق لعب فعلي بعد).

المارة، أعضاء العصابات، وسكان الجزر يستخدموا نفس النموذج والمكتبة (تم ترحيلهم من نظام Kenney القديم المكسور).

---

## 2. الخرائط والمناطق (خريطة واحدة متصلة، بوابات تدريجية)

| المنطقة | الاسم | شرط الفتح |
|---|---|---|
| المدينة الرئيسية | **Dravenport** | مفتوحة من البداية |
| حي ناطحات سحاب | Meridian Heights | ضمن Dravenport |
| حي فقير | Lowtown | ضمن Dravenport |
| حي سكني متوسط | Brookside | ضمن Dravenport |
| حي حكومي | Capitol Row | ضمن Dravenport |
| نهر + جسر | Vale River | ضمن Dravenport |
| مدينة متوسطة 1 | **Corvale** | بعد 3 مهام |
| مدينة متوسطة 2 | **Ashbrook** | بعد 6 مهام |
| صحراء/أطلال | **Dunmoor** (المنطقة الصحراوية) | بعد 5 مهام |
| غابة | **Greymoss Forest** | بعد 8 مهام |
| بحر وميناء | **Saltmere Bay** | بعد 12 مهمة |
| جزيرة 1 | Solace Isle | ضمن Saltmere Bay (وصول بالقارب فقط) |
| جزيرة 2 | Raven Isle | ضمن Saltmere Bay (وصول بالقارب فقط) |
| مترو | **The Underline** | محطتين (Meridian Heights ↔ Capitol Row) + قطار متحرك فعلي |

---

## 3. الأنظمة المبرمجة (Autoloads + سكربتات رئيسية)

### Autoloads (تشتغل طول اللعبة)
- `GameManager` — نقود، تقدم القصة، حفظ/تحميل (JSON محلي)
- `MissionManager` — 20 مهمة متسلسلة، صعوبة متصاعدة
- `WantedSystem` — نجوم مطاردة (0-5)، يستدعي شرطة تلقائياً
- `WeaponSystem` — 6 أسلحة نارية، فتح تدريجي حسب تقدم المهام
- `CharacterSelect` — بيانات Zade/Nyra
- `MedicalSystem` — إسعاف تلقائي عند صحة حرجة
- `AudioSettings` — مستوى صوت عام (بدون ملفات صوت فعلية بعد)
- `StoreSystem` — ملابس/دروع/سيوف/سيارات (شراء + خزانة مجانية من المهام)
- `AdSystem` — محاكاة إعلان مكافئ (وظيفي، مو شبكة إعلانات حقيقية)
- `ScreenshotCapture` — أداة تصوير شاشة محلية للاختبار البصري (F12) — **أُعيد إنشاؤها بعد ما كانت مفقودة (راجع سجل التعديلات)**

### أنظمة اللعب
- `car_controller.gd` — فيزياء سيارة حقيقية (VehicleBody3D) + ركوب/نزول (E)
- `player_character.gd` — مشي، تسلل (C)، قتال، تبديل مع السيارة
- `chase_camera.gd` — كاميرا سينمائية: تتبع + تدوير حر (سحب) + اهتزاز تصادم + FOV ديناميكي
- `police_ai.gd` + `police_spawner.gd` — شرطة تطارد، صعوبة متصاعدة
- `gang_member.gd` + `mission_enemy_spawner.gd` — أعداء يتولّدون فعلياً حسب كل مهمة
- `pedestrian_npc.gd` + `city_spawner.gd` — مارة يتمشوا، يهربوا من الفوضى، يتضرروا من التصادم (دهس = جريمة)
- `traffic_car.gd` — سيارات NPC على مسار
- `traffic_light_controller.gd` — إشارات مرور حقيقية (أحمر/أصفر/أخضر بتوقيت منطقي)
- `forest_animal.gd` / `sea_fish.gd` — حيوانات وأسماك حقيقية بحركة عشوائية/سباحة
- `boat_controller.gd` — قارب حقيقي قابل للسواقة
- `metro_train.gd` / `metro_station.gd` — قطار حقيقي متحرك + سفر سريع
- `area_gate.gd` — بوابات تفتح تلقائياً حسب تقدم المهام
- `touch_controls.gd` — تحكم لمسي كامل (غاز/فرامل/يمين/يسار/هاندبريك/تفاعل/تسلل) قابل للسحب والتكبير/التصغير، يُحفظ محلياً
- `character_model_loader.gd` (أداة مشتركة) — تحميل أي شخصية + دمج حركاتها تلقائياً وقت التشغيل

### الواجهات (UI)
- القائمة الرئيسية (مشهد 3D حي بخلفية متحركة)
- اختيار الشخصية
- HUD (نقود، مهمة، وقت، خريطة مصغرة)
- شاشة إعدادات (صوت، تحكم، خصوصية، حول + أزرار حفظ/تحميل)
- متجر موحّد (أسلحة/ملابس/دروع/سيوف/سيارات)

---

## 4. الأصول (كلها CC0 — مجانية تجارياً وشخصياً بدون قيود)

| المصدر | الاستخدام |
|---|---|
| Kenney Car Kit | سيارات، طرق، عناصر مدينة أساسية |
| Kenney Nature Kit | أشجار، صخور، صحراء، طرق ترابية |
| Kenney Graveyard Kit | أطلال، مقابر |
| Kenney Watercraft / Pirate Pack | قوارب |
| Quaternius Universal Base Characters | نماذج Zade/Nyra/NPCs |
| Quaternius Universal Animation Library | 42 حركة بشرية |
| Quaternius Train Pack | القطار |
| Quaternius Street Pack | إشارات مرور، أعمدة إنارة |
| Quaternius Animal/Fish Packs | حيوانات الغابة والبحر |
| Quaternius Modular Outfits | ملابس (محاولة تركيبها **فشلت** — مفصّل بالقسم 6) |

ملفات التراخيص الكاملة موجودة بمجلد `assets/LICENSE_*.txt` بالمستودع.

---

## 5. المهام (20 مهمة متسلسلة، تفتح تلقائياً بالترتيب)

توصيل → مطاردة شرطة → سرقة سيارة → عصابات → اقتحام مستودع → حرب عصابات → سطو بنك → مطاردة سريعة → سرقة شاحنة → انتقام → تسلل → مطاردة نخبة → سرقة مجوهرات → دفاع عن الحي → سباق سيارات مسروقة → اقتحام برج → هروب أخير → حرب كبرى → **المواجهة الأخيرة**.

كل مهمة: وقت محدد، مكافأة مالية، وعدد أعداء يتولّدون فعلياً (للمهام القتالية).

---

## 6. ✅ المُنجز بالكامل (شغال ومُختبر)

- قيادة واقعية + مشي + ركوب/نزول من السيارة
- 20 مهمة شغالة بصعوبة متصاعدة + توليد أعداء فعلي
- شرطة تطارد بذكاء اصطناعي حقيقي
- نظام أسلحة نارية (6 أسلحة) + متجر موحّد كامل
- خزانة مجانية (غنائم مهام) منفصلة عن الشراء
- تصادم فعلي بأضرار حقيقية (دهس مارة = جريمة)
- تسلل حقيقي يقلل مدى اكتشاف الشرطة/العصابات
- كاميرا سينمائية (تدوير حر، اهتزاز، FOV ديناميكي)
- خريطة استدلال (Minimap)
- Zade و Nyra بحركات بشرية حقيقية (مش T-Pose)
- مارة/عصابات/سكان جزر مرحّلين لنفس نظام الحركات الجديد
- قطار مترو حقيقي متحرك + محطتين
- إشارات مرور حقيقية بتوقيت منطقي
- حيوانات غابة وحياة بحرية حقيقية
- تحكم لمسي كامل قابل للتخصيص (سحب + تكبير/تصغير) ومحفوظ محلياً
- قائمة رئيسية سينمائية (مشهد 3D حي)
- شاشة إعدادات كاملة (صوت/تحكم/خصوصية/حول) + أزرار حفظ/تحميل
- المشروع بالكامل مرفوع على GitHub (كود + أصول)
- **بناء headless كامل (استيراد + تصدير Web + تصدير Linux) تم اختباره محلياً بصفر أخطاء (2026-09-21)**
- **قالب Android (APK Debug) مُعدّ بالكامل بـ`export_presets.cfg` + Gradle build مفعّل**
- **GitHub Actions workflow (`.github/workflows/godot-build.yml`) يبني APK تلقائياً عند كل push، ويرفعه كـartifact قابل للتحميل**

---

## 7. ❌ الناقص / غير المكتمل

| البند | الحالة بالتفصيل |
|---|---|
| **ملابس حقيقية على الشخصيات** | حاولنا تركيب زي "Peasant" من حزمة Quaternius فوق الجسم الأساسي — **فشلت المحاولة** (نتج تشوه + رجوع لـT-Pose جزئي)، تراجعنا عنها. الشخصيات حالياً بدون ملابس ظاهرة |
| **سلاح مرئي بيد الشخصية** | منطق الضرر شغال (Raycast) لكن بدون موديل مسدس/سيف 3D ظاهر فعلياً بإيد اللاعب |
| **صوت وموسيقى** | صفر ملفات صوتية بالمشروع بالكامل — `AudioSettings` جاهز تقنياً بس ما إله شي يشغّله |
| **دراجات نارية/هوائية** | غير موجودة — ما لقينا حزمة CC0 مناسبة |
| **حي Gearyard (الكراجات)** | اسم فقط بخطة التسميات، ما انبنى فعلياً بالخريطة |
| **تعبيرات وجه/جسم** | غير موجودة (المكتبة الجديدة فيها حركات جسم لكن بدون Blend Shapes للوجه مفعّلة) |
| **طقس (عواصف، مطر)** | غير موجود إطلاقاً |
| **قنوات صوت منفصلة (موسيقى/مؤثرات)** | البنية جاهزة بـ`AudioSettings` بس ما فيها أكتر من "Master" واحد |
| **مدن/جزر إضافية بتفاصيل أعمق** (مطاعم، كراجات فعلية) | غير موجودة |
| **تأكيد فعلي إن APK يشتغل على جهاز حقيقي** | الـworkflow جاهز ويبني APK على GitHub Actions، لكن لسا ما تأكد التشغيل الفعلي على هاتف (بما فيه جهاز المستخدم Huawei) |

---

## 8. ملاحظات تقنية مهمة اكتُشفت أثناء التطوير

1. **مشكلة T-Pose الأصلية**: نماذج Kenney البشرية الأولى ما كانت تحتوي AnimationPlayer إطلاقاً — تم حلها بإنشاء واحد يدوياً وربطه بالمكان الصحيح بالهيكل.
2. **مقياس FBX**: بعض الملفات تستورد بمقياس مضخّم — محاولة تصحيح آلية بـ 0.01 فشلت (صغّرت الشخصية لحجم شبه معدوم)، تراجعنا عنها. لم نحل هذا نهائياً بعد لبعض الأصول.
3. **جهاز المستخدم (هواوي)**: على الأغلب يشغّل HarmonyOS NEXT اللي قطع دعم تطبيقات أندرويد بالكامل — لهذا ملفات APK القديمة ما ثبّتت. الحل الحالي: بناء APK جديد تلقائياً عبر GitHub Actions (راجع القسم 6).
4. **الأصول الحقيقية مقابل حجم المشروع**: 127 ميجا أصول غير مضغوطة (259+ ملف نموذج 3D حقيقي)، Low-Poly بتصميم (أسلوب فني متعمد، مو نقص جودة).
5. **ملفات مفقودة اكتُشفت بالبناء الفعلي**: `project.godot` كان يشير لملفين غير موجودين بالمستودع (`scripts/debug/screenshot_capture.gd` و`scenes/cars/player_car.tscn`) — على الأغلب سقطوا أثناء رفع سابق من الجهاز المحلي. تم اكتشافهم فقط عبر تشغيل بناء headless فعلي (مو بمجرد نجاح "رفع" الملفات).

---

## 9. الخطوة التالية المقترحة (بالتسلسل المتفق عليه)

1. تأكيد نجاح أول build فعلي لـ APK على GitHub Actions، وتجربته على جهاز حقيقي
2. البحث عن حل صحيح لتركيب الملابس (أو قبول الشكل الحالي كنموذج أولي)
3. سلاح مرئي بيد اللاعب
4. صوت ومؤثرات (يحتاج مصدر CC0 جديد)
5. حي Gearyard
6. دراجات نارية/هوائية (يحتاج بحث أصول إضافي)

---

## 10. سجل التعديلات

كل تعديل جوهري يُضاف هنا بسطر واحد مع التاريخ.

- **2026-09-21** — فحص بناء headless كامل للمشروع: اكتُشف وأُصلح خطآن حقيقيان يمنعان تشغيل المشروع:
  - `scripts/debug/screenshot_capture.gd` كان مفقوداً رغم إنه مسجّل بـautoload في `project.godot` → أُعيد إنشاؤه.
  - `scenes/cars/player_car.tscn` كان مفقوداً رغم إنه مستخدم بـ`main.tscn` (مشهد سيارة اللاعب) → أُعيد بناؤه (VehicleBody3D + عجلات + ChaseCamera) بنفس نمط `police_car.tscn`.
  - بعد الإصلاحين: استيراد وتصدير (Web + Linux) محلياً بصفر أخطاء.
- **2026-09-21** — إضافة `export_presets.cfg` (Web, Linux, Android) وملف `.github/workflows/godot-build.yml` لبناء المشروع تلقائياً على GitHub Actions.
- **2026-09-21** — تعديل الـworkflow ليركّز حصراً على تصدير APK (Android Debug) بناءً على طلب المستخدم، باستخدام صورة `barichello/godot-ci:4.3` (فيها Android SDK + Gradle + مفتاح توقيع debug جاهزين).
- **2026-09-21** — تصحيح مسار الـworkflow بعد رفعه بالغلط لمجلد `github/workflows/` (بدون نقطة) بدل `.github/workflows/` — تم تصحيحه من طرف المستخدم والتأكد منه.
- **2026-09-21** — إضافة هذا الملف (`PROJECT_STATUS.md`) كمرجع حي داخل المشروع نفسه، بدل ما يبقى بس بمحادثة خارجية.
